# Upstream Tornado Cash instance contracts (tornado-core, solc 0.7.6), compiled
# separately so the e2e harness can deploy a fresh ETH instance on a fork.
[profile.default]
src = "src"
out = "out"
libs = []
solc = "0.7.6"
evm_version = "istanbul"
optimizer = true
optimizer_runs = 200
